# Azure AD Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env file to source control

# Azure AD Tenant Configuration
AZURE_TENANT_ID=your-tenant-id-here
AZURE_CLIENT_ID=your-client-id-here
AZURE_CLIENT_SECRET=your-client-secret-here

# Gateway Configuration
SERVER_PORT=8080

# Backend Service URLs
BACKEND_SPRING_BOOT_URL=http://localhost:8081
BACKEND_LEGACY_SPRING_URL=http://localhost:8082
BACKEND_GWT_URL=http://localhost:8083

# Session Store Configuration
# 
# Why shared session storage?
# The gateway uses OAuth2 session-based authentication. When running multiple
# gateway instances (Kubernetes, load-balanced production), a shared session
# store is REQUIRED to share session data across all instances. Without shared
# storage, users are forced to re-authenticate every time their request hits 
# a different instance.
#
# When to enable:
# - ✅ Production deployments with multiple gateway pods/instances
# - ✅ Kubernetes with replicas > 1
# - ✅ Load-balanced environments
# - ❌ Single instance development/testing (uses in-memory sessions)
#
# For detailed explanation, see ARCHITECTURE.md
#

# Option 1: Redis (Recommended for most use cases)
# Uncomment these to use Redis for session storage
# SPRING_REDIS_HOST=localhost
# SPRING_REDIS_PORT=6379
# SPRING_REDIS_PASSWORD=your-redis-password
# SPRING_SESSION_STORE_TYPE=redis

# Option 2: JDBC with Oracle Database
# Uncomment these to use Oracle database for session storage
# SPRING_DATASOURCE_URL=jdbc:oracle:thin:@localhost:1521/XEPDB1
# SPRING_DATASOURCE_USERNAME=session_user
# SPRING_DATASOURCE_PASSWORD=your-db-password
# SPRING_DATASOURCE_DRIVER_CLASS_NAME=oracle.jdbc.OracleDriver
# SPRING_SESSION_STORE_TYPE=jdbc
# SPRING_SESSION_JDBC_INITIALIZE_SCHEMA=always
